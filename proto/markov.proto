syntax = "proto3";

package markov;

import "graph.proto";

message Markov { graph.Graph graph = 1; }

message NGramMarkov {
  uint32 context_size = 1;
  graph.Graph graph = 2;
}

message NGramNodeMetadata { repeated uint64 context = 1; }
